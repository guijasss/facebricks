# Development
* first, think if there's a library to do the job. Do not reinvent the wheel.
* always write type annotations to function returns, parameters and variables.
* only import what you need. Instead of "import x", do "from x import y"
